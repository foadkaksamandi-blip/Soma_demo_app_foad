name: Build Buyer App

on:
  workflow_dispatch:      # فعال‌سازی دکمه "Run workflow"
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Buyer - build APK (release)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.25.2'
          channel: 'stable'

      - name: Flutter Pub Get
        run: flutter pub get

      - name: Clean build cache
        run: flutter clean && rm -rf ~/.pub-cache

      - name: Build APK (release)
        run: flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4   # نسخه جدید و فعال
        with:
          name: soma-buyer-release
          path: build/app/outputs/flutter-apk/app-release.apk
