plugins {
    id "com.android.application"
    id "kotlin-android"
}

android {
    namespace "ir.soma.demo.buyer"
    compileSdk 34

    defaultConfig {
        applicationId "ir.soma.demo.buyer"
        minSdk 23
        targetSdk 34
        versionCode 1
        versionName "1.0.0"
        multiDexEnabled true
    }

    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
            signingConfig signingConfigs.debug
        }
        debug {
            debuggable true
        }
    }

    packagingOptions {
        resources.excludes += ["META-INF/*"]
    }
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:1.9.24"
}
