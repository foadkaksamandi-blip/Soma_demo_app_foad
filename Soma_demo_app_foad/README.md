# Soma_demo_app_foad
SOMA Offline Banking Demo — Buyer & Merchant Apps (Foad version)

## 🎯 هدف
- ساخت دموی عملیاتی پرداخت آفلاین سوما (دو اپلیکیشن: خریدار و فروشنده)
- اجرای کامل پروژه فقط از طریق سایت GitHub با موبایل (بدون نیاز به کامپیوتر)
- قفل بودن نام‌ها، مسیرها و ساختار؛ هیچ حذف، تغییر یا خلاصه‌سازی بدون تأیید انجام نمی‌شود

---

## 📂 ساختار پروژه (قفل)

Soma_demo_app_foad/
 ├─ buyer-app/
 │   ├─ lib/main.dart
 │   └─ pubspec.yaml
 ├─ merchant-app/
 │   ├─ lib/main.dart
 │   └─ pubspec.yaml
 └─ .github/workflows/flutter-android.yml

- فقط همین ساختار پذیرفته است.
- همهٔ مسیرها و نام‌ها باید دقیقاً همین باشند (Case-Sensitive).
- هیچ فایلی بدون مستندسازی و هماهنگی اضافه/حذف/تغییر نمی‌شود.

---

## 🧩 اجزای اصلی

### ۱. اپ خریدار (buyer-app)
- نمایش موجودی
- انتخاب مبلغ خرید
- انتخاب روش پرداخت:
  - بلوتوث (Bluetooth)
  - QR کد
  - موجودی یارانه‌ای (نمایشی، فازهای بعد)
  - موجودی اضطراری ملی (نمایشی، فازهای بعد)
  - موجودی کیف پول رمز ارز ملی (نمایشی، فازهای بعد)
- ثبت لاگ تراکنش‌ها در حافظهٔ محلی (LocalDB)

### ۲. اپ فروشنده (merchant-app)
- نمایش موجودی فروشنده
- دریافت مبلغ از خریدار
  - از طریق بلوتوث
  - از طریق QR کد
- ثبت لاگ تراکنش‌ها

---

## 🔐 اصول قفل‌شده

1. **قفل نام پروژه:** Soma_demo_app_foad  
2. **قفل ساختار:** دو اپ جدا (buyer-app و merchant-app)  
3. **قفل تکنولوژی:** Flutter، Android، GitHub Actions  
4. **قفل عدم خلاصه‌سازی:** هیچ‌کدام از فایل‌ها به‌صورت نمونه یا ناقص ساخته نمی‌شوند.  
5. **قفل استقلال:** کل دمو باید فقط با همین ریپو و توضیحاتش قابل ساخت و اجرا باشد (بدون منبع خارجی).

---

## ⚙️ وابستگی‌ها (High Level)

- Flutter 3.24.x
- Android SDK 34
- Gradle سازگار با AGP 8.x
- Packages:
  - qr_flutter (برای تولید QR)
  - mobile_scanner یا qr_code_scanner (برای اسکن QR) — در فازهای بعد
  - flutter_blue_plus (برای بلوتوث واقعی) — در فازهای بعد
  - shared_preferences یا hive (برای ذخیره محلی) — بسته به طراحی

> در نسخهٔ اولیهٔ این دمو، برخی وابستگی‌ها فقط به صورت آماده‌سازی و Placeholder استفاده می‌شوند تا ساختار پروژه و Build در GitHub تست شوند.

---

## 🪜 مسیر اجرا (Batchها)
- **Batch 1:** این فایل + pubspec.yaml و main.dart برای هر دو اپ  
- **Batch 2:** صفحات پرداخت Bluetooth و QR  
- **Batch 3:** سرویس‌ها و مدل داده  
- **Batch 4:** ساخت Workflow برای بیلد APK در GitHub  
- **Batch 5:** مستندسازی نهایی و خروجی برای ارائه به بانک  

---

## 📌 وضعیت فعلی
این فایل چارچوب اصلی و قوانین قفل پروژه را تعریف می‌کند.  
قابلیت‌های پرداخت بلوتوث و QR در Batch‌های بعدی به‌صورت کامل افزوده خواهند شد.
