name: merchant_app
description: Soma Offline Demo — Merchant
publish_to: "none"
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter: { sdk: flutter }
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  permission_handler: ^11.3.1
  flutter_blue_plus: ^1.31.18
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.1.1

dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
