name: buyer_app
description: SOMA Offline Demo – Buyer app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # اسکن QR (نسخه سازگار با Kotlin 1.9.x و AGP 8.1.x)
  mobile_scanner: 5.1.1

  # اگر در پروژه از این‌ها استفاده می‌کنی نگه‌شان دار، وگرنه حذف‌شان کن
  intl: ^0.20.2
  flutter_blue_plus: ^1.31.17
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
