name: buyer_app
description: Soma Offline Demo — Buyer
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.19.0

  # QR / Camera
  mobile_scanner: ^3.5.7

  # Bluetooth (AGP 8 / Flutter 3.24.x compatible)
  flutter_blue_plus: ^1.31.17

  # Local key-value storage
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
