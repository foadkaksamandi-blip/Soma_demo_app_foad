name: buyer_app
description: SOMA Offline Demo – Buyer app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  mobile_scanner: ^3.2.0
  qr_flutter: ^4.1.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

# ✅ رفع خطای URI مربوط به flutter_localizations
dependency_overrides:
  flutter_localizations:
    sdk: flutter
