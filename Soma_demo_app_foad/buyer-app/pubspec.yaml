name: buyer_app
description: "SOMA Offline Banking Demo — Buyer App (Foad version)"
publish_to: "none"
version: 0.1.0+2

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  flutter_bluetooth_serial: ^0.4.0  # بلوتوث کلاسیک (RFCOMM) برای تبادل واقعی

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
