name: buyer_app
description: SOMA Offline Banking Demo - Buyer App
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # QR واقعی با دوربین (سازگار با AGP8 / SDK 36)
  mobile_scanner: ^6.0.11

  # تولید QR برای نمایش
  qr_flutter: ^4.1.0

  # بلوتوث واقعی
  flutter_blue_plus: ^1.32.5

  # UI
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
