name: buyer_app
description: SOMA Offline Demo – Buyer app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  # 👇 نسخه‌ی سازگار با Kotlin 1.9.x (برای CI فعلی)
  mobile_scanner: 3.2.0
  qr_flutter: ^4.1.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

# برای اطمینان از حل درست وابستگی‌ها
dependency_overrides:
  flutter_localizations:
    sdk: flutter
  mobile_scanner: 3.2.0
