name: buyer_app
description: Soma Offline Buyer App (QR + Bluetooth Real Mode)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_blue_plus: ^1.31.17
  mobile_scanner: ^3.2.0
  qr_flutter: ^4.1.0
  permission_handler: ^11.3.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/
