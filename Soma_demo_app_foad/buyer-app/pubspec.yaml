name: buyer_app
description: SOMA offline demo - buyer
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  intl: ^0.19.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^6.0.2
  permission_handler: ^11.3.1
  flutter_localizations:
    sdk: flutter
  # اگر از بلوتوث کلاسیک استفاده شده:
  flutter_bluetooth_serial: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
